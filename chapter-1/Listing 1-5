# Listing 1-5. Inspecting SQL generated by a QuerySet

# Import the Book model
from myapp.models import Book

# Retrieve all books
books = Book.objects.all()

# Print the generated SQL query
print(books.query)