# Listing 8-7 Using sync_to_async

from asgiref.sync import sync_to_async
 
async def book_count(request):
    count = await sync_to_async(Book.objects.count)()
    return JsonResponse({"count": count})