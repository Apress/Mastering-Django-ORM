# Listing 8-2 Async iteration

async for book in Book.objects.filter(is_published=True):
    print(book.title)

# Equivalent SQL:

SELECT "myapp_book"."id", "myapp_book"."title"
FROM "myapp_book"
WHERE "myapp_book"."is_published" = TRUE;