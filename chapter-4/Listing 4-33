# Listing 4-33 Using first method

first_book = Book.objects.order_by("published_date").first()
print(first_book.title, first_book.published_date)

# Equivalent SQL

SELECT book.id, book.title, book.author_id, book.price,
book.number_of_pages, book.published_date,
book.created_at
FROM book
ORDER BY book.published_date ASC
LIMIT 1;