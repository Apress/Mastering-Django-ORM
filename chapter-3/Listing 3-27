# Listing 3-27 endswith example

qs = Book.objects.filter(title__iendswith="azkaban")
print(list(qs))

# Output

[<Book: Harry Potter and the Prisoner of Azkaban>]

# Equivalent SQL

SELECT * FROM book WHERE UPPER(title) LIKE UPPER('%azkaban');