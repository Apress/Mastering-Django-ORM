# Listing 3-49 iso_week_day example

qs = Book.objects.filter(created_at__iso_week_day=1)  # Monday
print(list(qs))

# Output

[<Book: The Hobbit>]

# Equivalent SQL

SELECT * FROM book WHERE EXTRACT(ISODOW FROM created_at) = 1;