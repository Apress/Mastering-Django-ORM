# Listing 3-34 hour example

qs = Book.objects.filter(created_at__hour=14)
print(list(qs))

# Output

[<Book: The Hobbit>]

# Equivalent SQL

SELECT * FROM book WHERE EXTRACT(HOUR FROM created_at) = 14;