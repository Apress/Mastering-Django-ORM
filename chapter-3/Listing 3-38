# Listing 3-38 month example

qs = Book.objects.filter(created_at__month=6)
print(list(qs))

# Output

[<Book: The Hobbit>]

# Equivalent SQL

SELECT * FROM book WHERE EXTRACT(MONTH FROM created_at) = 6;