# Listing 3-50 year example

qs = Book.objects.filter(created_at__year=2025)
print(list(qs))

# Output

[<Book: The Hobbit>]

# Equivalent SQL

SELECT * FROM book WHERE EXTRACT(YEAR FROM created_at) = 2025;